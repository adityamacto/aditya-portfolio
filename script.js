const canvas=document.getElementById("stars"),ctx=canvas.getContext("2d");let stars=[];function resize(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);stars=Array.from({length:Math.min(190,innerWidth/6)},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.3+.2,v:Math.random()*.16+.03}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const s of stars){s.y+=s.v;if(s.y>innerHeight)s.y=-2;ctx.globalAlpha=.2+Math.random()*.65;ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)}resize();draw();addEventListener("resize",resize);document.getElementById("menu").onclick=()=>document.querySelector(".navlinks").classList.toggle("show");document.getElementById("year").textContent=new Date().getFullYear();